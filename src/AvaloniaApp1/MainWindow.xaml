<Window xmlns:iconPacks="clr-namespace:MahApps.Metro.IconPacks;assembly=AvaloniaIconPacks"
        xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="AvaloniaApp1.MainWindow"
        Title="AvaloniaApp1"
        Width="800"
        Height="600">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">

        <TextBlock Margin="0 10" Text="PackIconBoxIcons with AvaloniaUI" FontSize="22" FontWeight="SemiBold" />

        <iconPacks:PackIconBoxIcons Kind="RegularRecycle"
                                    Background="Silver"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Width="144" Height="144"
                                    Foreground="Black" />

        <TextBlock Margin="0 10" Text="{Binding #RotationSlider.Value, Mode=OneWay, StringFormat='RotationAngle {0}'}" FontSize="22" FontWeight="SemiBold" />
        <Slider Name="RotationSlider" Margin="0 2" Width="200" Minimum="0" Maximum="360" Value="45" SmallChange="1" LargeChange="10" />
        <TextBlock Margin="0 2" Text="{Binding #DurationSlider.Value, Mode=OneWay, StringFormat='Duration {0}'}" FontSize="22" FontWeight="SemiBold" />
        <Slider Name="DurationSlider" Margin="0 2" Width="200" Minimum="0" Maximum="10" Value="3" SmallChange="1" LargeChange="1" />
        <CheckBox Name="SpinCheckBox" Margin="0 2" Content="Spin" IsChecked="True" />
        <CheckBox Name="VisibilityCheckBox" Margin="0 2" Content="Visible" IsChecked="True" />

        <iconPacks:PackIconBoxIcons Kind="RegularCog"
                                    ClipToBounds="False"
                                    Background="Silver"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Width="144" Height="144"
                                    IsVisible="{Binding #VisibilityCheckBox.IsChecked, Mode=OneWay}"
                                    Spin="{Binding #SpinCheckBox.IsChecked, Mode=OneWay}"
                                    RotationAngle="{Binding #RotationSlider.Value, Mode=OneWay}"
                                    SpinDuration="{Binding #DurationSlider.Value, Mode=OneWay}"
                                    Foreground="Black" />

        <TextBlock Margin="0 10" Text="Flip = Vertical" FontSize="22" FontWeight="SemiBold" />

        <iconPacks:PackIconBoxIcons Kind="RegularRecycle"
                                    Background="Silver"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Width="144" Height="144"
                                    Flip="Vertical"
                                    Foreground="Black" />

    </StackPanel>
</Window>